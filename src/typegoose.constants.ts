/**
 * The name for the default database connection provider.
 * @internal
 */
export const DEFAULT_DB_CONNECTION_NAME = 'DefaultTypegooseConnection'

/**
 * The provider name for the provider that gives the name of the database name provider.
 * @internal
 */
export const TYPEGOOSE_CONNECTION_NAME = 'TypegooseConnectionName'

/**
 * The provider name for the typegoose module options.
 * @internal
 */
export const TYPEGOOSE_MODULE_OPTIONS = 'TypegooseModuleOptions'
